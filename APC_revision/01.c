#include <stdio.h>
#include <stdlib.h>

int main() {
  int num1, num2, num3, num4, cal;
  scanf("%d %d %d %d",&num1, &num2, &num3, &num4);
  cal = (num1 + num2 + num3 + num4);
  printf("%d\n",cal);
  return 0;
}
